@model IEnumerable<WebApplicationcheckwebconfig.Models.QuanLyCoffee>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaCoffee)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NameCoffee)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaCoffee)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NameCoffee)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            <button>@Html.ActionLink("Edit", "Edit", new { id=item.ID })</button> 
            <button>@Html.ActionLink("Details", "Details", new { id = item.ID })</button> 
            <button>@Html.ActionLink("Delete", "Delete", new { id = item.ID })</button>
        </td>
    </tr>
}
</table>
